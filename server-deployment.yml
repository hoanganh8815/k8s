appVersion: app/v1
kind: Deployment
spec:
	replicas: 3
	selector:
		matchLabels:
			component: server
	template:
		metadata:
			labels:
				component: server
		spec:
			containers:
				- name: server
				  image: stepengrider/multi-server
				  ports:
					- containerPort: 5000
					